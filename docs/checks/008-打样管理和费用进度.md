# 008 打样管理和费用进度检查记录

## PDF 对照

- `1.5.3.1 打样管理`：打样要求、内部打样单、外发工厂、打样费用、进度查询。
- `docs/05-实现步骤.md 3.1 打样管理`：打样单列表、新增/编辑、打样要求明细、内部打样单打印/导出、打样进度跟踪、打样费用登记、打样费用支付申请。
- `docs/00-需求范围.md 样品管理`：打样管理、打样费用、进度查询。

## 本次实现范围

后端：

- `GET /api/v1/sample/requests`：打样单列表，支持编号/客户/产品/供应商/要求/明细模糊查询，支持状态和客户过滤。
- `POST /api/v1/sample/requests`：新增打样单，并把客户打样要求转为内部打样单明细。
- `GET /api/v1/sample/requests/{request_id}`：打样单明细、内部打样单明细、进度记录和费用记录。
- `POST /api/v1/sample/requests/{request_id}/progress`：新增打样进度，并同步打样单状态。
- `POST /api/v1/sample/requests/{request_id}/fees`：登记打样费用。
- `POST /api/v1/sample/requests/{request_id}/fees/{fee_id}/payment-request`：发起打样费用付款申请入口，生成 `SAMPLE-FEE-` 付款申请号。
- SQLAlchemy async model：`SampleRequest`、`SampleRequestLine`、`SampleRequestProgress`、`SampleFee`。
- Repository 返回 typed row。
- Service 负责权限校验、状态校验、去向校验、状态流转、费用付款申请号生成和数据权限过滤。
- FastAPI 路由显式声明 `response_model=ApiResponse[...]`。
- 打样管理菜单权限：`sample:request:view`、`edit`、`view_all`、`fee:edit`。

前端：

- React 左侧菜单接入“打样管理”。
- React 打样管理页支持列表、搜索、状态筛选、客户筛选、新增打样单、进度更新、费用登记和付款申请入口。
- 打样去向覆盖：内部打样、外发工厂。
- 打样状态覆盖：草稿、已发送、进行中、已完成、已取消。
- 业务主管可访问打样管理；财务角色无打样管理菜单且接口返回 `403`。
- 页面沿用 ERP 工作区布局，便于业务员和跟单人员持续查看打样进度。

## 完整性检查

| 要求 | 状态 | 证据 |
| --- | --- | --- |
| 打样单列表 | 已实现 | `GET /api/v1/sample/requests`，前端“打样单列表”表格 |
| 打样单创建 | 已实现 | `POST /api/v1/sample/requests`，E2E 新增后回显 |
| 客户打样要求转内部打样单 | 已实现基础能力 | `SampleRequest.requirements` + `SampleRequestLine` |
| 可关联客户 | 已实现 | `customer_id`、`customer_name` 字段 |
| 可关联供应商 | 已实现 | `supplier_id`、`supplier_name` 字段 |
| 可关联商品 | 已实现 | `product_id`、`product_code`、`product_name` 字段 |
| 可关联业务员 | 已实现 | `sales_user_id`、`sales_user_name` 字段 |
| 发送打样间/外发工厂 | 已实现基础去向 | `destination=in_house/factory` |
| 打样进度更新 | 已实现 | `POST /progress`，E2E 更新为进行中 |
| 打样状态流转 | 已实现 | 新增进度时同步打样单 `status` |
| 打样费用登记 | 已实现 | `POST /fees`，E2E 登记 USD 120.50 |
| 打样费用付款发起 | 已实现入口 | `POST /payment-request` 生成 `SAMPLE-FEE-` 申请号 |
| 打样日期/客户/状态查询 | 已实现主体 | 当前实现客户/状态/文本查询；日期范围后续补 |
| 内部打样单打印/导出 | 后续依赖 | 待打印模板和导出模板模块完成后接入 |
| 正式财务付款单 | 后续依赖 | 待付款申请/付费管理模块完成后接入正式单据 |

## 自动化验证

- API 契约：`backend/tests/api/test_sample_requests.py`
- Repository 契约：`backend/tests/repositories/test_sample_request_repository.py`
- Service 契约：`backend/tests/services/test_sample_request_service.py`
- 权限菜单回归：`backend/tests/api/test_auth.py`、`backend/tests/services/test_auth_service.py`
- 架构约束：`backend/tests/architecture/test_fastapi_layers.py`
- React 前端构建：`npm run build`
- 前端端到端：`node scripts/e2e-sample-requests.mjs`

## 端到端证据

- 截图：`artifacts/e2e-sample-requests.png`
- E2E 覆盖：
  - 登录业务主管账号。
  - 点击“打样管理”菜单。
  - 等待 `/api/v1/sample/requests` 返回。
  - 新增外发工厂打样单，并录入内部打样单明细。
  - 等待 `/api/v1/sample/requests` 201 和列表刷新。
  - 更新打样进度为“外发工厂/进行中”。
  - 等待 `/api/v1/sample/requests/{request_id}/progress` 201。
  - 登记打样费用。
  - 等待 `/api/v1/sample/requests/{request_id}/fees` 201。
  - 发起打样费用付款申请。
  - 等待 `/payment-request` 成功并显示 `SAMPLE-FEE-` 申请号。

## 可用性检查

- 打样列表、状态筛选、客户筛选和新增表单在同一工作区可见。
- 打样详情集中展示客户、产品、去向、状态、日期、供应商和要求。
- 进度记录和费用记录位于同一明细区，方便跟踪“打样进度 + 费用处理”。
- 费用表内可直接发起付款申请，减少跨页面跳转。
- 财务角色不会看到打样管理菜单，避免越权入口。

## 后续依赖

- 付款申请/付费管理模块完成后，把 `SAMPLE-FEE-` 入口替换为正式付款申请单。
- 打印模板模块完成后，补内部打样单打印和导出。
- 日期范围筛选组件完成后，补按打样日期区间查询。
- 样品登记模块已提供样品台账和跟单节点事件，后续补打样完成自动转样品记录。
