# Generate a unique value for each deployment, for example:
# openssl rand -hex 32
AUTH_SECRET_KEY=replace-with-a-random-secret

# Keep disabled outside explicit demo environments.
SEED_DEMO_DATA=false

NGINX_PORT=8080
